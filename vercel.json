{
  "installCommand": "pnpm install --prod=false",
  "buildCommand": "pnpm --filter @workspace/algorace run build",
  "outputDirectory": "artifacts/algorace/dist/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
